# Task: Implement POST in React (local todos with json-server) - COMPLETED

## Steps:
1. [x] Update API.jsx: Clean up code, implement GET and POST for http://localhost:3001/todos using json-server, add input form for new todo title, display todos list with loading/error.
2. [ ] Test json-server: Run `cd ../baitap/vite-project && json-server --watch db.json --port 3001`.
3. [ ] Start Vite dev server: `cd ../baitap/vite-project && npm run dev`.
4. [ ] Test POST functionality in browser.
5. [x] Complete task.
